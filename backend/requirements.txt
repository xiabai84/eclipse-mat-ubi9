# MAT Analysis Service — Python dependencies
beautifulsoup4>=4.12.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
lxml>=5.0.0        # optional but faster HTML parser for BeautifulSoup
python-multipart>=0.0.9  # required for FastAPI File/Form (multipart upload)
